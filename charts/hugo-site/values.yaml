replicaCount: 2
image:
  repository: ghcr.io/zoonderkins/hugo-k8s-dashboard
  tag: 38c4a31c4db6cae81e9f59c073372f3cba8c1620
  pullPolicy: Always
  # Pull secrets for private image registry
  pullSecrets:
    - name: regcred

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: false