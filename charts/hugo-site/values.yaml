replicaCount: 2
image:
  repository: ghcr.io/zoonderkins/hugo-k8s-dashboard
  tag: ad188881c22eabc56abde708a0090c9524718bd9
  pullPolicy: Always
  # Pull secrets for private image registry
  pullSecrets:
    - name: regcred

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: false