replicaCount: 1
image:
  repository: ghcr.io/zoonderkins/hugo-k8s-dashboard
  tag: 3a75187bb0f1b628df5ef92f9aa1957757e1b1b1
  pullPolicy: Always
  # Pull secrets for private image registry
  pullSecrets:
    - name: regcred

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: false
  className: ""
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  hosts:
    - host: k8s.edoo.dev
      paths:
        - path: /
          pathType: Prefix
  tls: []
  #  - secretName: k8s-edoo-dev-tls
  #    hosts:
  #      - k8s.edoo.dev
