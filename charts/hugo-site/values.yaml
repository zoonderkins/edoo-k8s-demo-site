replicaCount: 1
image:
  repository: ghcr.io/zoonderkins/hugo-k8s-dashboard
  tag: b423a2d5e045fb8676fc443141f5a73d847fbab5
  pullPolicy: Always
  # Pull secrets for private image registry
  pullSecrets:
    - name: regcred

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: false
  className: ""
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  hosts:
    - host: k8s.edoo.dev
      paths:
        - path: /
          pathType: Prefix
  tls: []
  #  - secretName: k8s-edoo-dev-tls
  #    hosts:
  #      - k8s.edoo.dev
