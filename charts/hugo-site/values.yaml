replicaCount: 1
image:
  repository: ghcr.io/zoonderkins/hugo-k8s-dashboard
  tag: 26f444245bfac6c5d4fc539aa375b35c563b20ba
  pullPolicy: Always
  # Pull secrets for private image registry
  pullSecrets:
    - name: regcred

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: false
  className: ""
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  hosts:
    - host: k8s.edoo.dev
      paths:
        - path: /
          pathType: Prefix
  tls: []
  #  - secretName: k8s-edoo-dev-tls
  #    hosts:
  #      - k8s.edoo.dev
